
@model GrantCountyAs400.Web.ViewModels.BuildingVM.StateBuildingFeesDetailsViewModel
@{
    ViewData["Title"] = "State Building Code";
}

<h2>State Building Code</h2>

<div>
    <div class="entity-details row">
        <div class="entity-info clearfix">
            <partial name="_Header" for="@Model.BasicInfo" />
            <div class="col-xs-6">
                <table class="table">
                    <tbody>
                        <tr>
                            <td><strong>Total Value </strong></td>
                            <td class="text-right"><span> ? </span></td>
                        </tr>
                        <tr>
                            <td><strong>State Bldg Code Fee </strong></td>
                            <td class="text-right"><span> @Html.DisplayFor(model => model.StateBuildingExtendedAmount) </span></td>
                        </tr>
                    </tbody>
                </table>
            </div>
            <div class="col-xs-6">
                <table class="table">
                    <thead>
                        <tr>
                            <td>Number Of Units</td>
                            <td>Extended Amount</td>
                        </tr>
                    </thead>
                    
                    <tr>
                        @for (int i = 0; i < Model.CoutOfLargestList; i++)
                        {

                            <td class="text-left">
                                @if (i < Model.NumberOfUnits.Count)
                                {
                                    @Html.DisplayFor(model => model.NumberOfUnits)
                                }
                            </td>
                            @if(i < Model.ExtendedAmount.Count)
                            {
                                <td class="text-left"> @Html.DisplayFor(model => model.ExtendedAmount) </td>
                            }
                        }
                    </tr>
                    
                </table>
            </div>
        </div>
    </div>
</div>
